{
  "name": "xterm-webtuigui",
  "version": "0.1.0",
  "module": "./api.ts",
  "exports": {
    ".": "./api.ts",
    "./build": "./build.ts"
  },
  "type": "module",
  "private": true,
  "scripts": {
    "build:manifest": "bun run index.ts build:manifest",
    "build:wasm": "bun run index.ts build:wasm",
    "build:web": "bun run index.ts build:web",
    "build": "bun run index.ts build",
    "dev": "bun run index.ts dev",
    "test": "bun test"
  },
  "dependencies": {
    "@xterm/addon-fit": "0.11.0",
    "@xterm/xterm": "6.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
