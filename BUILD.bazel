package(default_visibility = ["//visibility:public"])

native.sh_test(
    name = "native_gate",
    srcs = ["Tools/bazel/native_gate.sh"],
    tags = [
        "exclusive",
        "local",
        "no-sandbox",
    ],
)
