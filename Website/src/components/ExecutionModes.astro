---
---
<section class="modes" id="runtimes">
  <div class="shell">
    <header class="head">
      <span class="eyebrow"><span class="num">/02</span>Three runtimes, one App</span>
      <h2>Authored once. Hosted anywhere a Swift compiler reaches.</h2>
      <p class="lede">
        The same <span class="mono">@main struct App</span> declaration flows
        into terminal-native binaries, into the browser via WASI with
        <span class="mono">SharedArrayBuffer</span>-backed stdin, and into
        host-managed embeddings inside SwiftUI windows or any DOM element.
        The framework owns the cells; the host owns the chrome.
      </p>
    </header>

    <div class="grid">
      <article class="tile primary">
        <div class="tile-head">
          <div class="tile-id">
            <span class="badge">native</span>
            <span class="path mono">Runners/SwiftTUICLI</span>
          </div>
          <h3>Terminal-native</h3>
          <p>
            <span class="mono">RunLoop</span> over the alternate screen. ANSI / sanitized OSC 8
            hyperlinks / Kitty / Sixel negotiated against the live capability profile. Unix signal
            handling, mouse reporting, and pty-backed secondary scenes ship by default.
          </p>
        </div>
        <pre class="code mono"><code><span class="kw">import</span> <span class="ty">SwiftTUICLI</span>

<span class="at">@main</span> <span class="kw">struct</span> <span class="ty">DemoApp</span>: <span class="ty">App</span> &#123;
  <span class="kw">var</span> body: <span class="kw">some</span> <span class="ty">Scene</span> &#123;
    <span class="ty">WindowGroup</span>(<span class="s">"Deploy Dashboard"</span>) &#123;
      <span class="ty">BuildSummary</span>()
    &#125;
  &#125;
&#125;</code></pre>
        <ul class="meta mono">
          <li><span class="dot on"></span><span>shipped · macOS 15+ · Linux · iOS 18+</span></li>
          <li><span class="dot"></span><span class="dim">Android · in tree, not yet certified</span></li>
        </ul>
      </article>

      <article class="tile">
        <div class="tile-head">
          <div class="tile-id">
            <span class="badge">wasi</span>
            <span class="path mono">Runners/SwiftTUIWASI</span>
          </div>
          <h3>Browser, via WASI</h3>
          <p>
            Build the same target to <span class="mono">wasm32-wasi</span>. The
            <span class="mono">WASIRunner</span> emits a scene manifest the host loads through
            <span class="mono">GUI/WebHost</span> — no terminal emulator, no xterm.js, no
            postMessage shim.
          </p>
        </div>
        <pre class="code mono"><code><span class="cm">// build</span>
swift build --swift-sdk swift-6.3.1-RELEASE_wasm \
  --target <span class="ty">WebExampleApp</span>

<span class="cm">// host</span>
<span class="kw">await</span> createWebHostApp(&#123;
  manifestUrl: <span class="s">"./scene-manifest.json"</span>,
  sceneRuntimeFactory: createWasmSceneRuntimeFactory(...)
&#125;)</code></pre>
        <ul class="meta mono">
          <li><span class="dot on"></span><span>requires COOP: same-origin · COEP: require-corp</span></li>
        </ul>
      </article>

      <article class="tile wide">
        <div class="split">
          <div class="half">
            <div class="tile-id">
              <span class="badge">embed</span>
              <span class="path mono">GUI/SwiftUIHost</span>
            </div>
            <h3>Inside SwiftUI</h3>
            <p>
              Retain a <span class="mono">HostedSceneSession</span> in your SwiftUI view.
              Theme tokens swap at runtime; the authored TUI never sees the host.
            </p>
            <pre class="code mono"><code><span class="ty">SwiftUIHost</span>(scene: deployScene)
  .terminalTheme(.solarizedDark)
  .frame(minWidth: <span class="n">560</span>, minHeight: <span class="n">320</span>)</code></pre>
          </div>
          <div class="half">
            <div class="tile-id">
              <span class="badge">embed</span>
              <span class="path mono">GUI/WebHost</span>
            </div>
            <h3>Inside any DOM element</h3>
            <p>
              Drop a <span class="mono">.terminal-shell</span> div into an Astro, Remix,
              Next, or vanilla page. The host owns chrome &mdash; the framework owns the cells.
            </p>
            <pre class="code mono"><code>&lt;div class=<span class="s">"terminal-shell"</span>
     data-scene=<span class="s">"gallery"</span>&gt;&lt;/div&gt;
&lt;script type=<span class="s">"module"</span> src=<span class="s">"/host.js"</span>&gt;&lt;/script&gt;</code></pre>
          </div>
        </div>
      </article>
    </div>
  </div>
</section>

<style>
  .modes {
    padding: 96px 0 56px;
    border-top: 1px solid var(--line);
    margin-top: 64px;
  }
  .head { max-width: 760px; margin-bottom: 48px; }
  .head h2 { margin-top: 16px; }
  .head .lede { margin-top: 18px; max-width: 70ch; }

  .grid {
    display: grid;
    grid-template-columns: 1.18fr 1fr;
    grid-auto-rows: auto;
    gap: 18px;
  }
  .tile.primary { grid-column: 1; }
  .tile.wide { grid-column: 1 / -1; }

  .tile {
    position: relative;
    border: 1px solid var(--line-strong);
    border-radius: 14px;
    background: linear-gradient(180deg, var(--panel) 0%, var(--bg-soft) 100%);
    padding: 26px 26px 22px;
    overflow: hidden;
  }
  .tile.primary::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: radial-gradient(640px 280px at 0% 0%, rgba(52, 211, 153, 0.08), transparent 60%);
  }
  .tile-head { position: relative; }
  .tile-id {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 14px;
  }
  .badge {
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    padding: 3px 7px;
    border-radius: 4px;
    background: var(--panel-2);
    border: 1px solid var(--line-strong);
    color: var(--ink-2);
  }
  .path { color: var(--ink-3); font-size: 11.5px; }
  .tile h3 {
    font-size: 22px;
    letter-spacing: -0.018em;
    margin-bottom: 10px;
  }
  .tile p {
    font-size: 14px;
    line-height: 1.55;
    color: var(--ink-2);
    max-width: 60ch;
  }

  .code {
    margin: 18px 0 0;
    padding: 14px 16px;
    border-radius: 10px;
    background: #060606;
    border: 1px solid var(--line);
    font-size: 12.5px;
    line-height: 1.7;
    color: var(--ink);
    overflow-x: auto;
    white-space: pre;
  }
  .code .kw { color: #d28cff; opacity: 0.85; }
  .code .ty { color: #6ddfff; opacity: 0.86; }
  .code .at { color: #f59e0b; }
  .code .s { color: var(--accent); }
  .code .n { color: #f59e0b; }
  .code .cm { color: var(--ink-4); font-style: italic; }

  .meta {
    margin: 18px 0 0;
    padding: 0;
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 6px;
    font-size: 11.5px;
    color: var(--ink-3);
  }
  .meta li { display: inline-flex; align-items: center; gap: 9px; }
  .meta .dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--ink-4);
  }
  .meta .dot.on { background: var(--accent); box-shadow: 0 0 0 3px rgba(52, 211, 153, 0.18); }
  .meta .dim { color: var(--ink-4); }

  .split {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0;
  }
  .half { padding: 4px 22px; }
  .half + .half {
    border-left: 1px solid var(--line);
  }

  @media (max-width: 1080px) {
    .grid { grid-template-columns: 1fr; }
    .tile.primary { grid-column: 1; }
    .tile.wide { grid-column: 1; }
    .split { grid-template-columns: 1fr; }
    .half + .half { border-left: 0; border-top: 1px solid var(--line); padding-top: 22px; margin-top: 4px; }
  }
  @media (max-width: 720px) {
    .tile { padding: 22px 18px 18px; }
  }
</style>
