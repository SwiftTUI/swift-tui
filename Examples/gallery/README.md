# Component Gallery

This example app is a compact catalog of the public `TerminalUI` surface.

It is designed to show the new dense automatic chrome in a scroll-safe layout,
with representative samples of controls, collections, charts, and appearance
overrides.

## Run

```bash
cd Examples/gallery
swift run gallery-demo
```

## Test

```bash
cd Examples/gallery
swift test
```
